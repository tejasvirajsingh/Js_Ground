let age = 4;

let Reason = age > 18 ? "Eligible for vote" : "Not allowed for Vote , Because under 18";

console.log("Reason: " + Reason);  //Reason: Not allowed for Vote , Because under 18


/* 
✅ Ternary Operator Syntax (Remember This)

  -> condition ? value_if_true : value_if_false


Example:

age > 18 ? "Eligible" : "Not Eligible"

Now complete the day 4..
*/